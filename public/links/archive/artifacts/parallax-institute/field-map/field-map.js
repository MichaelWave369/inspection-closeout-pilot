const lanes=document.getElementById('lanes'),summary=document.getElementById('summary'),errorBox=document.getElementById('error'),search=document.getElementById('search');let projects=[],activeRole='all';const roles={stewardship:{title:'Stewardship',description:'Governance, continuity, evidence, institutional memory, and public explanation.'},research:{title:'Research',description:'Hypotheses, formal models, inquiry, human systems, and bounded interpretation.'},prototyping:{title:'Prototyping',description:'Software, physical systems, field infrastructure, simulations, and practical builds.'},'public-benefit':{title:'Public Benefit',description:'Experiences and systems intended to return human, cultural, ecological, resilience, or livelihood value.'}};const overrides={'carbon-loop':'public-benefit','coherence-bridge':'public-benefit','parallax-institute':'stewardship','parallax-archive-terminal-system':'stewardship','alpha-ledger-omega':'stewardship','parallax-census':'stewardship','sovereign-office-architecture':'stewardship','graph-cinema':'research','crystalline-temporal-prism':'research','nested-bubble-field-theory':'research'};function roleFor(project){if(overrides[project.id])return overrides[project.id];if(project.category==='foundation'||project.category==='governance')return'stewardship';if(project.category==='research')return'research';if(project.category==='software'||project.category==='physical')return'prototyping';return'public-benefit'}function esc(value){return String(value??'')}function card(project){const a=document.createElement('a');a.className='card';a.href=`../../../project/?id=${encodeURIComponent(project.id)}`;const strong=document.createElement('strong');const title=document.createElement('span');title.textContent=project.title;const status=document.createElement('small');status.textContent=project.status||'Status not recorded';strong.append(title,status);const p=document.createElement('p');p.textContent=project.tagline||project.summary||'Open the canonical Archive record.';a.append(strong,p);return a}function render(){const needle=search.value.trim().toLowerCase();const filtered=projects.filter(project=>{const role=roleFor(project);const roleMatch=activeRole==='all'||role===activeRole;const text=[project.title,project.id,project.mark,project.status,project.tagline,...(project.aliases||[])].join(' ').toLowerCase();return roleMatch&&(!needle||text.includes(needle))});summary.textContent=`Showing ${filtered.length} of ${projects.length} canonical Archive records. Roles are curatorial and may change as evidence and project purpose evolve.`;lanes.replaceChildren();Object.entries(roles).forEach(([id,meta])=>{if(activeRole!=='all'&&activeRole!==id)return;const section=document.createElement('section');section.className='lane';const header=document.createElement('header');const h3=document.createElement('h3');h3.textContent=meta.title;const p=document.createElement('p');p.textContent=meta.description;header.append(h3,p);const cards=document.createElement('div');cards.className='cards';const items=filtered.filter(project=>roleFor(project)===id);if(items.length)items.forEach(project=>cards.append(card(project)));else{const empty=document.createElement('p');empty.className='empty';empty.textContent='No matching records in this role.';cards.append(empty)}section.append(header,cards);lanes.append(section)})}document.querySelectorAll('[data-role]').forEach(button=>button.addEventListener('click',()=>{activeRole=button.dataset.role;document.querySelectorAll('[data-role]').forEach(item=>item.setAttribute('aria-pressed',String(item===button)));render()}));search.addEventListener('input',render);async function init(){try{const archive=await window.ParallaxArchiveCore.load();projects=archive.projects.slice();render()}catch(error){errorBox.hidden=false;errorBox.textContent=`The live Archive Core could not be loaded. ${esc(error.message)}`;summary.textContent='No institutional map was generated.'}}init();